# GitHub token for build-time stats + live API on Vercel (read-only public repo access is enough)
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx

# Optional: used by local dev if GITHUB_TOKEN is not set
# VITE_GITHUB_TOKEN=
